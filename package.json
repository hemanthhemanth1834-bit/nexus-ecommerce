{
  "name": "nexus-ecommerce",
  "private": true,
  "version": "1.0.0",
  "description": "NEXUS — Premium Full-Stack E-Commerce Platform",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "build:server": "cd server && npm run build",
    "start": "cd server && npm start",
    "setup": "cd server && npm install && npx prisma generate && npx prisma migrate dev && npx tsx src/seed.ts && cd ../client && npm install",
    "seed": "cd server && npx tsx src/seed.ts",
    "migrate": "cd server && npx prisma migrate dev",
    "db:reset": "cd server && npx prisma migrate reset --force && npx tsx src/seed.ts"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
